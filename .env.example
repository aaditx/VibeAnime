# VibeAnime — Environment Variables
#
# Copy this file to .env.local and fill in the values before running locally.
# For production (Vercel), set these in your project's Environment Variables settings.

# ──────────────────────────────────────────
# Auth (next-auth) — REQUIRED
# ──────────────────────────────────────────
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=your-secret-here

# ──────────────────────────────────────────
# Public base URL — REQUIRED for production
# Used for Open Graph meta image URLs.
# ──────────────────────────────────────────
NEXT_PUBLIC_BASE_URL=https://vibeanime.vercel.app

# ──────────────────────────────────────────
# Consumet API  — optional
# If you self-host the Consumet API, set this.
# Defaults to the public instance.
# ──────────────────────────────────────────
# CONSUMET_API_URL=https://your-consumet-instance.vercel.app
