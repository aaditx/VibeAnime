# VibeAnime — Environment Variables
#
# Copy this file to .env.local and fill in the values before running locally.
# For production (Vercel), set these in your project's Environment Variables settings.

# ──────────────────────────────────────────
# Resend — REQUIRED for password reset emails
# Sign up free at https://resend.com → API Keys → Create
# ──────────────────────────────────────────
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# The verified sender address (must match your verified Resend domain)
EMAIL_FROM=VibeAnime <noreply@vibeanime.app>

# Your email — only this email can access /admin
ADMIN_EMAIL=you@gmail.com

# ──────────────────────────────────────────
# MongoDB — REQUIRED
# Create a free M0 cluster at https://cloud.mongodb.com
# Cluster → Connect → Drivers → copy the connection string.
# Replace <password> with your DB user password.
# ──────────────────────────────────────────
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/vibeanime?retryWrites=true&w=majority

# ──────────────────────────────────────────
# Auth (next-auth) — REQUIRED
# ──────────────────────────────────────────
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=your-secret-here

# ──────────────────────────────────────────
# Public base URL — REQUIRED for production
# Used for Open Graph meta image URLs.
# ──────────────────────────────────────────
NEXT_PUBLIC_BASE_URL=https://vibeanime.app

# ──────────────────────────────────────────
# Consumet API  — optional
# If you self-host the Consumet API, set this.
# Defaults to the public instance.
# ──────────────────────────────────────────
# CONSUMET_API_URL=https://your-consumet-instance.vercel.app
